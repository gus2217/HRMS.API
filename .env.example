# Copy to .env and adjust as needed. Used by docker-compose.yml (Redis/OTel only).
# Postgres is EXTERNAL — point dev.sh at your server via these vars:
#   DB_HOST=your-postgres-host
#   DB_PORT=5432
#   DB_NAME=jacana_hrms
#   DB_USER=jacana
#   DB_PASS=your-password
